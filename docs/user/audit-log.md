# The audit log

*For district administrators and system administrators.*

Every change to members, districts, regions and user accounts is recorded automatically by the database: who made it, when, and the values before and after. Nobody can edit or delete entries, not even administrators.

Open **Audit log**, filter by record type or kind of change, and click an entry to see exactly which fields changed. District administrators see their own district's entries; system administrators see all of them.

Passwords are never recorded, not even in scrambled form.
